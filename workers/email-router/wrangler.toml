name = "aevoy-email-router"
main = "src/index.ts"
compatibility_date = "2024-09-25"
account_id = "5b63e25e3235482bc883cacd3cf189f7"

# Email routing configuration
# After deploying:
# 1. Set secrets (see below)
# 2. Go to Cloudflare dashboard > Email > Email Routing
# 3. Enable Email Routing for aevoy.com
# 4. Add catch-all rule pointing to this worker
#
# Set secrets with:
#   npx wrangler secret put AGENT_URL              # e.g. https://agent.aevoy.com
#   npx wrangler secret put AGENT_WEBHOOK_SECRET   # same value as in agent server .env
#   npx wrangler secret put SUPABASE_URL           # https://xxx.supabase.co
#   npx wrangler secret put SUPABASE_SERVICE_KEY   # sb_secret_xxx

[vars]
# Production agent URL
AGENT_URL = "http://77.42.31.185:3001"
SUPABASE_URL = "https://eawoquqgfndmphogwjeu.supabase.co"
